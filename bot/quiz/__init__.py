# -*- coding: utf-8 -*-
# quiz package — table declarations + QuizJobs singleton are added once the
# runtime modules exist (see docs/plans/2026-06-17-aoe2-quiz.md, Task C1/F1).
# Kept as a bare stub during the pure-core TDD phase so importing bot.quiz.scoring
# / .pool / .view does not pull in nextcord.
