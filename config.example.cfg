# -*- coding: utf-8 -*-

# Your discord credentials
DC_BOT_TOKEN = ""
DC_CLIENT_ID = 1234567890
DC_CLIENT_SECRET = ""
DC_INVITE_LINK = ""
DC_OWNER_ID = 1234567890  # your user id (owner of the bot)
DC_SLASH_SERVERS = [] # Fill if you want slash commands only in those dc servers
FLAGSHIP_GUILD_IDS = [] # Guild ids that keep full replay retention instead of the multi-tenant default 'lean'

PUBOBOT_USER_ID = 177022387903004673  # User ID of the original Pubobot to sync ELO from
LOBBYBOT_USER_ID = 1230652255875629058  # AOE2LobbyBOT user ID for civ sync

# Product mode: hosted offers the Discord product without replay parsing;
# self_hosted permits replay parsing on the operator's own compute.
DEPLOYMENT_MODE = "self_hosted"  # "hosted" or "self_hosted"

# Replay analysis is paused by default. The first switch downloads and parses;
# the other switches independently guard legacy presentation surfaces. They apply
# only in self_hosted mode and should stay False for the lightweight product.
REPLAY_INGEST_ENABLED = False
REPLAY_POSTGAME_CARDS_ENABLED = False
SCOUTING_REPORT_ENABLED = False
RANK_ELO_CHART_ENABLED = False
REPLAY_DASHBOARD_ENABLED = False

DB_URI = "" # mysql://user:password@hostname:port/database"
DB_POOL_MAX_SIZE = 2 # tiny workload: bound concurrent MySQL connections
DB_IDLE_CLOSE_SECONDS = 60 # release free sockets so Railway MySQL can sleep
LOG_LEVEL = "DEBUG" # CHAT, DEBUG, COMMANDS, INFO or ERRORS
FILE_LOG_ENABLED = False # Opt in to duplicate stdout logs into local files
STATUS = "NammaAoe2Bot" # bot presence string

# Web server
WS_ENABLE = False
WS_HOST = ""
WS_PORT = 443
WS_OAUTH_REDIRECT_URL = ""
WS_ROOT_URL = ""  # Public URL of the web dashboard (required for OAuth login, e.g. "https://your-app.up.railway.app")
WS_SSL_CERT_FILE = ""
WS_SSL_KEY_FILE = ""
