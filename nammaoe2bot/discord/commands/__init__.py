# -*- coding: utf-8 -*-
"""Handlers for the pickup game's own commands. Import the module you want.

It used to be a star-import shelf — nine `from .x import *` lines — which made
every call site read `commands.something` and say nothing about where that
handler lived. nammaoe2bot/discord/slash.py names the module now.
"""
