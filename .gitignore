config.cfg
saved_state.json
logs/
__pycache__/
*.pyc
*.pyo
locales/compiled/
data/logs/
# One-off CSV dump from the original Pubobot, used by
# utils/import_pubobot_export.py as input. Not a redistributable
# artifact — do not check in.
export-apr10/
# Test venv scratch space (created for local test runs against the
# system Python 3.14 which lacks pytest).
.pytest_cache/
# Raw scratch capture from utils/lobby_spike.py (Phase 0) — ~1MB of ambient
# public lobbies. The committed golden fixture is a small curated test123
# lifecycle, added separately in Phase 1.
tests/fixtures/lobby_events.json
