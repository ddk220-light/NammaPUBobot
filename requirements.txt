# Pinned 2026-04-11 from the versions the Railway builder installed in
# commit 288edfd. Bump deliberately, not accidentally — unpinned deps are
# how "worked fine yesterday" becomes "broken at 3am". Indirect deps are
# left unpinned so pip can pull compatible transitives.
nextcord==2.6.0
aiomysql==0.3.2
glicko2==2.1.0
trueskill==0.4.5
emoji==2.15.0
prettytable==3.17.0
aiohttp==3.13.5
cryptography==46.0.7
# Sentry is loaded conditionally via SENTRY_DSN env var — see PUBobot2.py.
# Without a DSN, the init becomes a no-op but the package still loads to
# keep imports non-conditional. If you never set SENTRY_DSN you pay no
# runtime overhead; setting it enables exception capture via the task
# supervisor's _task_done_callback.
sentry-sdk==2.18.0
